add the api key in .env file
create venv
"streamlit run app.py" paste this in the terminal to open the streamlit web for organising files
then select the project folder which should be reorganised and then copy and paste its url in the web
after pressing reorganise and getting the preview, then confirm to change the file locations within the specified folder.
